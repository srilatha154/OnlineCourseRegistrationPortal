# OnlineCourseRegistrationPortal
This project follows a 3-tier architecture with React frontend, Spring Boot backend, and MySQL database. Students can view courses and enroll in them. The backend handles logic like preventing duplicate registrations and checking seat availability. Enrollment table manages the many-to-many relationship between students and courses.
